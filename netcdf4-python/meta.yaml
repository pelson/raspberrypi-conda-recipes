package:
  name: netcdf4  # Note, for consistency with anaconda, this is not netcdf4-python.
  version: 1.1.3

source:
  git_tag: v1.1.3rel
  git_url: https://github.com/Unidata/netcdf4-python.git

requirements:
  build:
    - python
    - numpy
    - libnetcdf
  run:
    - python
    - numpy
    - libnetcdf

test:
  imports:
    - netCDF4

