package:
   name: conda-build
   version: 1.17.0

source:
  git_url: https://github.com/conda/conda-build
  git_tag: 1.17.0

requirements:
   build:
     - python
   run:
     - python
     - patchelf
     - conda

build:
  number: 0

test:
   commands:
     - conda build --help
   imports:
     - conda_build
